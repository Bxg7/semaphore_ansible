---
- hosts: all
  

  
  tasks:

  - name: Slurp hosts file
    slurp:
      src: /home/jelle/rsynclogs/rsync-backup-j-*.log
    register: slurpfile

  - debug: msg="{{ slurpfile['content'] | b64decode }}"


